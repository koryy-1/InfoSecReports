@page
@model InfoSecReports.Pages.EventOfObjects.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Редактировать:</h1>

<h4>Меропритие по защите</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="EventOfObject.Id" />
            <div class="form-group">
                <label>Имя организации</label>
                <input asp-for="EventOfObject.NameOfCompany" class="form-control" />
                <span asp-validation-for="EventOfObject.NameOfCompany" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>Имя мероприятия</label>
                <input asp-for="EventOfObject.NameOfEvent" class="form-control" />
                <span asp-validation-for="EventOfObject.NameOfEvent" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Обновить" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
