@page
@model InfoSecReports.Pages.ScriptOfObjects.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Добавить:</h1>

<h4>Сценарий по проверке объекта</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label>Имя организации</label>
                <input asp-for="ScriptOfObject.NameOfCompany" value="@Model.NameOfCompany" class="form-control" readonly/>
                <span asp-validation-for="ScriptOfObject.NameOfCompany" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>Имя сценария</label>
                <select asp-for="ScriptOfObject.NameOfScript" class="form-select" aria-label="Выберите мероприятие" required>
                    @{
                        foreach (var item in Model.Script)
                        {
                            <option value=@item.Name>@Html.DisplayFor(modelItem => item.Name)</option>
                        }
                    }
                </select>
                <span asp-validation-for="ScriptOfObject.NameOfScript" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Добавить" class="btn btn-primary" />
            </div>
            <a class="btn btn-secondary" onclick="history.go(-2);"><font color="white">Назад</font></a>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
